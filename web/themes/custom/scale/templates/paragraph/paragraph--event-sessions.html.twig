{% embed 'scale:container' with {containerWidth: 'fixed'} %}
  {% block content %}
    <div class="mb-8">
      {% set event_id = paragraph.field_event.target_id %}
      {{ drupal_view('session_teasers', 'embed', event_id) }}
    </div>
  {% endblock %}
{% endembed %}
