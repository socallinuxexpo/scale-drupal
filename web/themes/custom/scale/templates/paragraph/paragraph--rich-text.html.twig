
{# TODO: Not all rich text will have a blue background. Add UI for this. Or add new components. #}

{% embed 'scale:container' with {
  classes: 'bg-primary-50 p-7 mb-7'
} %}
  {% block content %}
    {{ drupal_field('field_rich_text', 'paragraph', paragraph.id(), {label: 'hidden'}) }}
  {% endblock %}
{% endembed %}
