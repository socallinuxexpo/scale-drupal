{% set classes = [
    'component',
    classes ?? '',
    containerWidth ? 'container-' ~ containerWidth : 'container-fluid',
] %}

<div{{ attributes.addClass(classes) }}>
    <div class="inside">
        {% block content %}
            {{ content }}
        {% endblock %}
    </div>
</div>
