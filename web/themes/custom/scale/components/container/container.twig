{% set classes = [
    'component',
    classes ?? '',
] %}

<div{{ attributes.addClass(classes) }}>
    <div class="inside">
        {% block content %}
            {{ content }}
        {% endblock %}
    </div>
</div>
